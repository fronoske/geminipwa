// @ts-nocheck -- Optional local Lorebook data is loaded before this runtime script.
// The repository intentionally contains no user Lorebook contents.
const BUILTIN_LOREBOOKS = Array.isArray(globalThis.LOCAL_LOREBOOKS)
    ? globalThis.LOCAL_LOREBOOKS
    : [];
